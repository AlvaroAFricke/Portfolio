---
import Data from "../data.json";
import ProyectosItem from "../components/ProyectosItem.astro";

import ast from "./icons/skills/astro.astro";
---

<section id="Proyectos" class="proyectos px-3 h-1/3">
  <div class="pb-8 pt-6 flex items-center align-middle flex-col">
    <h1 class="text-4xl font-bold text-center text-gray-900">Proyectos</h1>

    <div class="grid grid-cols-1 md:px-48 gap-5 mt-5 h-full w-full">
      {
        Data.proyectos.map((elemento) => (
          <ProyectosItem
            titulo={elemento.nombre}
            descripcion={elemento.descripcion}
            tecnos={elemento.tecnologias}
            codigo={elemento.codigo}
            enlace={elemento.enlace}
          />
        ))
      }
    </div>
  </div>
</section>
